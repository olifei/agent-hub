steps:
- name: gcr.io/cloud-builders/docker
  args:
    - build
    - -f
    - mcp_server/Dockerfile
    - -t
    - ${_IMAGE_URI}
    - .
- name: gcr.io/cloud-builders/docker
  args: [push, '${_IMAGE_URI}']
images:
- ${_IMAGE_URI}
options:
  machineType: E2_HIGHCPU_8
timeout: 1200s
